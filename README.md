neural-tic-tac-toe
========

```bash
#instalar ruby, npm
npm install -g bower
npm install -g phantomjs
bundle
```

Correr servidor:
`rails server`

```javascript
//todo: escribir una descripción
```
